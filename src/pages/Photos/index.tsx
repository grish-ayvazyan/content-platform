import MasonryGrid from "@/components/Masonry/MasonryGrid";
import { PhotosWrapper } from "@/pages/Photos/styles.ts";

const Photos = () => {
    return (
        <PhotosWrapper>
            <MasonryGrid />
        </PhotosWrapper>
    );
};

export default Photos;
